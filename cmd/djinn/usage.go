package main

var usage = `djinn - Simple build system

Usage:

  djinn [options...]

Options:

  -a, --artifacts <dir>  The directory to store build artifacts
  -d, --driver <file>    The driver config file to use
  -m, --manifest <file>  The manifest YAML file to use
  -o, --objects <dir>    The directory to look in for object placement
  -s, --stage <stage>    The stage to run

  --help  Display this usage message`
