namespace: djinn
driver:
  type: qemu
  image: centos/7
objects:
- data
env:
- LOCALE=en_GB.UTF-8
stages:
- clean
jobs:
- stage: clean
  commands:
  - tr -d '0-9' data > data.cleaned
  artifacts:
  - data.cleaned
